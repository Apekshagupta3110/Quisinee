# Client-side (Vite)
VITE_PUSHER_KEY=
VITE_PUSHER_CLUSTER=
VITE_PUSHER_FORCE_TLS=true
VITE_PUSHER_UPDATE_ENDPOINT=/api/pusher/update-cart

# Server-side (Pusher REST trigger endpoint)
PUSHER_APP_ID=
PUSHER_KEY=
PUSHER_SECRET=

# MongoDB
MONGODB_URI=mongodb://localhost:27017/quisine
PUSHER_CLUSTER=
